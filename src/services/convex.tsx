import type React from 'react';

// Simple placeholder provider until we set up the backend properly
export function ConvexClientProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}